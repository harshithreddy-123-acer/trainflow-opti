// Update this page (the content is just a fallback if you fail to update the page)

import TrainControlDashboard from '../components/TrainControlDashboard';

const Index = () => {
  return <TrainControlDashboard />;
};

export default Index;
